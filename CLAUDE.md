# Project instructions

## Branching / integration

- **Always merge completed work into `claude/world-cup-pool-optimizer-dXYWi`** for this
  repository. That branch is the canonical integration branch.
- Do feature work on the session's designated feature branch, then merge it into
  `claude/world-cup-pool-optimizer-dXYWi` (fast-forward when possible) and push, unless
  the user explicitly says otherwise.
